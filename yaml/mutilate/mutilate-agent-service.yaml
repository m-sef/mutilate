# Headless service: gives each StatefulSet pod a unique, resolvable DNS name.
# clusterIP: None means no VIP is assigned, DNS resolves directly to pod IPs.
apiVersion: v1
kind: Service
metadata:
  name: mutilate-agent-service
  namespace: mutilate
spec:
  clusterIP: None
  selector:
    app: mutilate-agent
  ports:
  - protocol: TCP
    port: 5556
    targetPort: 5556